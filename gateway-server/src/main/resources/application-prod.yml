spring:
  cloud:
    nacos:
      discovery:
        server-addr: prod:8848
        username: nacos
        password: nacos
        namespace: spring-cloud-demo
        group: prod
    gateway:
      enabled: true
      discovery:
        locator:
          enabled: true