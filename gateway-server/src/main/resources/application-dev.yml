spring:
  cloud:
    nacos:
      discovery:
        server-addr: dev:8848
        username: nacos
        password: nacos
        namespace: spring-cloud-demo
        group: dev
    gateway:
      enabled: true
      discovery:
        locator:
          enabled: true