spring:
  cloud:
    nacos:
      discovery:
        server-addr: dev:8848
        username: nacos
        password: nacos
        namespace: dev
        group: dev
    gateway:
      enabled: true
      discovery:
        locator:
          enabled: true